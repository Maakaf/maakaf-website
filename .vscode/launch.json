{
  "version": "0.2.0",
  "configurations": [
    
    {
      "command": "npm run dev",
      "name": "Run Project",
      "request": "launch",
      "type": "node-terminal",
      "cwd": "${workspaceFolder}"
    }
  ]
}
